// Generated by CoffeeScript 1.6.3
module.exports = function(tokens, textAreaSVG) {
  var dataToView, fontFamily, fontSize, height, textArea, token, tokenToViewable, width, _i, _len, _results;
  console.log('viewporting started');
  width = textAreaSVG.attr('width');
  height = textAreaSVG.attr('height');
  fontFamily = 'Helvetica';
  fontSize = '18px';
  dataToView = [];
  textArea = textAreaSVG.append('svg');
  tokenToViewable = function(token) {
    var visualToken;
    visualToken = textArea.append('text').style('text-anchor', 'middle').attr("dominant-baseline", "central").style('fill', '#EEEEEE').style('font-family', fontFamily).style('font-size', fontSize).attr('y', -100).attr('x', -100).style('fill-opacity', '0');
    visualToken.text(token);
    console.log(token);
    return console.log(visualToken.node().getComputedTextLength());
  };
  _results = [];
  for (_i = 0, _len = tokens.length; _i < _len; _i++) {
    token = tokens[_i];
    _results.push(tokenToViewable(token));
  }
  return _results;
};
